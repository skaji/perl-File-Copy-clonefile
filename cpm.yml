prereqs:
  runtime:
    requires:
      perl: { version: 'v5.20' }
  test:
    requires:
      Test::LeakTrace:
  configure:
    requires:
      Dist::Build: { version: '0.013' }
      ExtUtils::Builder::Compiler: { version: '0.020' }
      ExtUtils::Constant:
