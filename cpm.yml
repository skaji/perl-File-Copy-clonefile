prereqs:
  runtime:
    requires:
      perl: { version: 'v5.20' }
  test:
    requires:
      Test::LeakTrace:
  configure:
    requires:
      Module::Build:
      ExtUtils::Constant:
      Devel::CheckCompiler: { version: '0.07' }
