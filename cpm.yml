prereqs:
  runtime:
    requires:
      perl: { version: 'v5.20' }
  test:
    requires:
      Test::LeakTrace:
  configure:
    requires:
      Dist::Build: { version: '0.006' }
      ExtUtils::Constant:
      Devel::CheckCompiler: { version: '0.07' }
